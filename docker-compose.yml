services:
  db:
    image: mongo
    container_name: mongo
    ports:
    - "27017:27017"
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongosh localhost:27017/serviceapp --quiet
    restart: always
    volumes:
      - ./db/data:/data/db
      - ./db/mongodump:/dump